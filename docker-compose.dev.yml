# Dev-only overlay. Publishes the no-auth markitdown sidecar to localhost so a
# locally-run `bun dev` (outside the compose network) can reach it. Applied
# explicitly via `-f`, so the production compose stays internal-only.
services:
  markitdown:
    ports:
      - "127.0.0.1:4281:4281"
